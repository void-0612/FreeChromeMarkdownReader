# Markdown Reader Demo

这是一个用于测试插件的 Markdown 文件。加载扩展后，右侧应该出现免费目录。

## 章节一

普通段落、**粗体**、_斜体_、`inline code` 和 [链接](https://example.com)。

### 子章节

- 列表项 A
- 列表项 B
- 列表项 C

## 任务列表

- [x] 渲染 Markdown
- [x] 生成目录
- [ ] 打包发布

## 表格

| 功能 | 状态 |
| --- | --- |
| 标题 | 已支持 |
| 目录 | 已支持 |
| 主题切换 | 已支持 |

## 代码块

```js
function hello(name) {
  return `Hello, ${name}`;
}
```

## 引用

> 目录功能应该是阅读器的基础能力，不该被锁在付费层里。

## 最后一节

滚动到这里时，目录里的当前项应该高亮。
